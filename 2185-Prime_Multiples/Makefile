export PROBLEM = 2185-HPTHINK-TESTE
export CPPFLAGS = -DONLINE_JUDGE -std=c++17 -O2
export OUTPUT = > /dev/null 2>&1
export TEST = ../test/entry02.txt ../test/entry03.txt

all:
	+$(MAKE) -C all
	#+$(MAKE) -C training
	#+$(MAKE) -C control

clean:
	rm all/*.exe training/*.exe control/*.exe
